/* 
1. create a firebase project in in firebase.google.com
2. npm install firebase
3. make a initialize file of firebase in src folder(firebase.init.js) and export auth
4. enable auth system from firebase settings authentication
 */